SimpleDnsApp

is a simple Rails application that uses the Rackspace Cloud DNS API Ruby Wrapper written by github.com/sosedoff.
It provides a local interface for a rackspace cloud user can more easily manage (CRUD) their DNS, domains, and records


STILL WORKING...